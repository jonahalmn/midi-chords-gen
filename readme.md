# MIDI EMITTER

This project generates procedural melodies.

- Mac only (CoreMidi, CoreFoundation)

### Deps

- rtmidi

#### Setup & Build

``` brew install rtmidi ```

``` cmake . ```

``` cmake --build . ```

### Run Program

``` bin/main ```